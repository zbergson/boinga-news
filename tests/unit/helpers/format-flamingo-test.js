import { formatFlamingo } from 'wsj-coding-test/helpers/format-flamingo';
import { module, test } from 'qunit';

module('Unit | Helper | format flamingo');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatFlamingo([42]);
  assert.ok(result);
});
