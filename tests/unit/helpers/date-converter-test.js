import { dateConverter } from 'wsj-coding-test/helpers/date-converter';
import { module, test } from 'qunit';

module('Unit | Helper | date converter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateConverter([42]);
  assert.ok(result);
});
