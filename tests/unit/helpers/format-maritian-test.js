import { formatMaritian } from 'wsj-coding-test/helpers/format-maritian';
import { module, test } from 'qunit';

module('Unit | Helper | format maritian');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatMaritian([42]);
  assert.ok(result);
});
